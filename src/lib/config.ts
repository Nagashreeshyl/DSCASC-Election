// Application-wide configuration that is safe to expose (no secrets here).
export const ADMIN_EMAILS = [
  "powrrskanda@gmail.com",
  "nagashreeshyl@gmail.com"
].map((e) => e.toLowerCase());

export const APP_NAME = "DSCASC Election Platform";
export const DEFAULT_STUDENT_WEIGHT = 70;
export const DEFAULT_AUTHORITY_WEIGHT = 10;
